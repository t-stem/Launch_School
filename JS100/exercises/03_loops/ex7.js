/* The following code keeps looping forever. (You can hit Ctrl-C to stop it.) 
Why is that? Also modify it so that it stops after the first iteration. */

// it keeps looping because it has no condition for termination between the ; ;. A
// adding a condition that will evaluate as truthy here will terminate the loop

for (let i = 0; i < 1; i += 1) {
  console.log("and on");
}