/* Write a function that returns the last element of an input array. For example: */

function first(array) {
    return array[array.length - 1];
}

console.log(first(['Earth', 'Moon', 'Mars'])); // 'Mars'